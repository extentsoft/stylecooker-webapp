module.exports = {
  calculate: function(subtotal,district,done){
    // implemented later
  },
  getDiscount: function(subtotal, uid, done){
    // 1. get history (frequency)
    // 2. get history (spending)
    // 3. formulate discount
    // 4. return discount
    //done({discount: 15});
  }
}
